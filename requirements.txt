# ============================================================
# GenWealth — Project Dependencies
# ============================================================

# --- Phase 1: Quant Engine ----------------------------------
jupyter
pandas>=2.0
yfinance>=0.2
matplotlib
seaborn
scikit-learn>=1.4
statsmodels
transformers>=4.40        # FinBERT sentiment model
joblib
torch>=2.1
feedparser
requests
beautifulsoup4
scipy

# --- Phase 2: RL Planner Engine -----------------------------
stable-baselines3[extra]>=2.3
gymnasium>=0.29
shimmy>=0.2.0

# --- Phase 3: Advisor Engine (Hybrid RAG + Multi-LLM) -------
pymongo[srv]>=4.6          # MongoDB Atlas driver
sentence-transformers>=2.7 # Local embeddings (all-MiniLM-L6-v2)
python-dotenv>=1.0         # .env loading
ddgs>=9.0                  # DuckDuckGo Search (keyless news)
numpy>=1.26
google-generativeai>=0.7   # Gemini LLM client
groq>=0.9                  # Groq LLM client

# --- Phase 4: Production API & Vanilla JS Frontend ----------
fastapi>=0.115             # API framework
uvicorn[standard]>=0.30    # ASGI server
sse-starlette>=2.0         # Server-Sent Events helper
aiofiles>=23.0             # Async static file serving (StaticFiles)
python-multipart>=0.0.9    # Form-data parsing (multipart uploads)